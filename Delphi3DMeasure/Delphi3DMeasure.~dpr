program Delphi3DMeasure;

///<summary>
/// Главный файл проекта Delphi 3D Measure.
/// Предназначение: Точка входа приложения, инициализация формы.
/// Статистика:
///   - Типы: 0
///   - Константы: 0
///   - Переменные: 1 (Application)
///   - Функции/Процедуры: 0
///   - Классы: 0
/// Модули: uTypes, uMath, uUtils, uRenderer, uMeasure, uMainForm
///</summary>

uses
  Forms,
  uMainForm in 'uMainForm.pas' {MainForm},
  uTypes in 'uTypes.pas',
  uMath in 'uMath.pas',
  uUtils in 'uUtils.pas',
  uRenderer in 'uRenderer.pas',
  uMeasure in 'uMeasure.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TMainForm, MainForm);
  Application.Run;
end.
